# spring nodes

This sample keeps only pkg.json so the repository stays small and readable.

For a real FETCH zip, this folder should contain the full installed Dynamo package folder exactly as it appears under the Dynamo package cache, including any in, dyf, doc, and extra folders required by the package.
