name: Validate manifests

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate package and graph manifests
        shell: pwsh
        run: ./scripts/validate-manifests.ps1
