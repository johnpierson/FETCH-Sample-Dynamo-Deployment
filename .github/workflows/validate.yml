name: Validate FETCH zip source

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate FETCH zip source
        shell: pwsh
        run: ./scripts/validate-fetch-zip-source.ps1
